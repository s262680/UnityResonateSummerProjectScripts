using UnityEngine;
using System.Collections;

public class HitEffectDestroyScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	Destroy(this.gameObject,0.1f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
